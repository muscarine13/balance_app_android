<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Balance" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/my_background</item>
        <item name="colorPrimaryDark">@color/my_pane_background</item>
        <!-- Secondary brand color. -->

        <!-- Status bar color. -->

        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.Balance.NoActionBar.AppWidgetContainer" parent="">
        <item name="appWidgetBackgroundColor">@color/my_background</item>
        <item name="appWidgetTextColor">@color/white</item>
    </style>

</resources>